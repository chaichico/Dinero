<script setup lang="ts">const route = useRoute(); const router = useRouter(); const title = ref(''); const amount = ref<number | null>(null); function save() { router.push(`/splits/${route.params.id}`) }</script>
<template><main class="page"><NuxtLink :to="`/splits/${route.params.id}`" class="text-link">← Group</NuxtLink><h1 class="display">Add an expense.</h1><form class="form" @submit.prevent="save"><div class="field"><label>What was it?</label><input v-model="title" placeholder="Dinner, taxi…" required></div><div class="field"><label>Total amount</label><input v-model.number="amount" type="number" min="1" required></div><div class="card"><strong>Split equally</strong><p class="muted" style="margin-bottom:0;font-size:13px">You can adjust each person’s share next.</p></div><button class="button accent button-block">Review split</button></form></main></template>
